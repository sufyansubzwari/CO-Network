fragment userFragment on User {
  _id
  createdAt
  services
  profile {
      info {
          name
          lastName
          image
      }
  }
  subscriptions {
    endpoint
    keys {
      auth
      p256dh
    }
  }
}
