mutation CreateUser($entity: UserInput!) {
    user(user: $entity) {
        _id
        createdAt
        services
        views
    }
}

mutation DeleteUser($id: String!) {
    deleteUser(_id: $id) {
        _id
    }
}

mutation UpdateSignUpStatus($id: String!, $status: Boolean!) {
    updateSignUpStatus(_id: $id, status: $status) {
        _id
        profile {
            isSignUp
        }
    }
}