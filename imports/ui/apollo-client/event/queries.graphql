
query GetEvents($limit: Int, $events: EventsInputSearch) {
  events(limit: $limit, events: $events) {
    _id
    title
    description
    venueName
    image
    owner {
      _id
    }
    category {
      label
      value
      name
    }
    entity
    views
  }
}
