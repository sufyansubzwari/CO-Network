mutation DeleteNotification($id: String, $all: Boolean) {
  deleteNotification(_id: $id, all: $all) {
    _id
  }
}

mutation UpdateNotification($notification: NotificationsInput) {
  notification(notification: $notification) {
    _id
  }
}