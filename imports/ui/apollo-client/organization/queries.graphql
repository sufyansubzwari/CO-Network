
query GetEvents($limit: Int, $filter: String) {
  events(limit: $limit, events: $filter) {
    _id
    title
    description
    venueName
    image
    owner {
      _id
    }
    category {
      label
      value
      name
    }
    entity
    views
  }
}
